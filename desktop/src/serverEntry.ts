/**
 * Точка входа серверного бандла: что именно десктопная оболочка берёт у сервера.
 *
 * Сервер собирается в один файл (`server.mjs`) рядом с главным процессом:
 * иначе в установщик пришлось бы класть каталог `node_modules` монорепозитория
 * со всеми симлинками рабочих пространств, которые установщик разворачивает
 * по-своему на каждой платформе.
 *
 * Здесь перечислено ровно то, что оболочке нужно: собрать приложение, закрыть
 * базу при выходе и починить материалы, зависшие в обработке после того, как
 * прошлый запуск закрыли посреди распознавания скана.
 */
export { buildApp } from '@lt/server/dist/src/app.js';
export { closeDb } from '@lt/server/dist/src/db/connection.js';
export { recoverStuckMaterials } from '@lt/server/dist/src/services/materialService.js';
